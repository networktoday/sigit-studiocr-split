# SendGrid API Key per l'invio delle email di notifica
# Ottieni la tua chiave su https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=la_tua_chiave_api_sendgrid

# Porta del server (default: 5000)
PORT=5000
